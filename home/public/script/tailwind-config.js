(function (window) {
  window.tailwind.config = {
    // 配置暗黑模式
    darkMode: 'selector',
    // 扩展样式主题
    theme: {
      extend: {
        colors: {
          clifford: '#da373d',
        }
      }
    }
  }
})(window)